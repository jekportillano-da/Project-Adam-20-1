# Core FastAPI dependencies
fastapi>=0.116.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6
aiofiles>=24.1.0
jinja2>=3.1.2

# HTTP client (httpx preferred over requests for async)
httpx>=0.26.0

# Configuration and environment
python-dotenv>=1.0.0
pydantic>=2.0.0

# Authentication and security (only if using auth)
passlib[bcrypt]>=1.7.4
python-jose[cryptography]>=3.3.0
email-validator>=2.2.0

# AI integration
openai>=1.98.0

# Database (lightweight, only what we actually use)
sqlalchemy>=2.0.0

# Development tools (only essential ones)
pytest>=7.0.0
pytest-asyncio>=0.21.0

# Security
cryptography>=41.0.0
